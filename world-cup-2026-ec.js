const getWorldCupMatches = (
  idMatchesContainer,
  idCarouselBtnPrev,
  idCarouselBtnNext,
  idCardCounter,
  strClasses,
  jsonPath
) => {
  const classes = JSON.parse(strClasses);
  console.log(
    "PROBANDO JS MUNDIAL:",
    idMatchesContainer,
    classes,
    idCarouselBtnPrev,
    idCarouselBtnNext,
    idCardCounter,
    jsonPath
  );
};
